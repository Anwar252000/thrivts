# The .NET API (Thrivts.Api) is the ONLY thing this app talks to — including auth. Supabase is
# never contacted directly from the browser; the API proxies to Supabase Auth internally.
VITE_API_BASE_URL=http://localhost:5275
